pluginManagement {
  repositories {
    gradlePluginPortal()
  }
}
buildCache {
  local {
    setEnabled(true)
    setDirectory(file("${rootProject.projectDir.absolutePath}/.gradle/cache"))
  }
}

include("common", "functional", "system", "infrastructure", "stress", "integrational")
