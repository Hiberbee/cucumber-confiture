pluginManagement {
  repositories {
    gradlePluginPortal()
  }
}
buildCache {
  local {
    enabled = true
    directory = file("${rootProject.projectDir.absolutePath}/.gradle/cache")
  }
}

rootProject.name = "cucumber-confiture"

includeBuild("common")
include("functional", "system", "infrastructure", "integrational")

