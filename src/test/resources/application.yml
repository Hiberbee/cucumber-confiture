logging:
  level:
    root: warn
spring:
  main:
    banner-mode: 'off'
  application:
    name: cucumber-confiture
cucumber:
  slack:
    channel: cucumber
    enabled: true
    icon: ''
    token: ${SLACK_TOKEN}
