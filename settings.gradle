pluginManagement {
  repositories {
    gradlePluginPortal()
  }
}
buildCache {
  local {
    enabled = true
    directory = file("${rootProject.projectDir.absolutePath}/.gradle/cache")
  }
}

rootProject.name = "cucumber-confiture"

include("functional", "system", "infrastructure")
includeBuild("common")

